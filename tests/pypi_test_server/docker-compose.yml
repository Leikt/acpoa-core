---
version: '3.9'
services:
  pypiserver:
    container_name: pypiserver_test
    image: pypiserver/pypiserver
    restart: always
    ports:
      - 8080:8080
    volumes:
      - ./packages:/data/packages